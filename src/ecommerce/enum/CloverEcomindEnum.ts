export enum CloverEcomindEnum {
    /* a payment where the customer enters their card details for a single transaction. */
    ECOM = 'ecom',
    /* a payment where the merchant enters the customer's card details received over the phone or by mail. */
    MOTO = 'moto',
}
